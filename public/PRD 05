# 全局时间轴、音频（BGM/台词）智能踩点匹配
## 一、 第一性原理拆解：AI 视频的“时间轴”本质是什么？
传统剪辑软件（如 Premiere/剪映）的时间轴是**“容器”，假设用户已经拥有了所有素材，只是在里面进行拼接。 而 AI 短剧/绘本平台的时间轴是“标尺与约束器”。它的核心逻辑不是“剪辑”，而是“对齐（Alignment）”**：通过时间戳（Timecode），强制让【画面动作】、【台词口型】和【音乐节拍】发生物理上的同步。
---
## 二、 全局时间轴（Timeline）的轨道架构设计
基于多视角推演（音视频工程师 + AI 算法视角），为了满足短剧和绘本的需求，你的底层数据结构必须是一个**“四轨并行的非线性序列”**：
|轨道层级|核心功能|AI 自动化逻辑|用户干预维度|
|V1: 视觉主轨 (主副分镜)|承载生成的视频片段/图片垫片。|系统根据下方的【台词时长】反向计算并锁定视频生成的秒数。|拖拽调整顺序、替换片段、添加转场效果。|
|A1: 台词/人声轨 (Dialogue)|承载 TTS（文本转语音）生成的角色对白。|AI 自动区分角色标签（如 @角色A，@角色B），分配不同音色库。|语速微调、情绪选项（愤怒/悲伤）、手动切片。|
|A2: 音效轨 (SFX)|承载动作音效与环境音。|提取分镜文本中的动词词向量（如“下雨”、“拔剑”），自动在关键帧插入音效库资产。|音量大小调节、手动删减误判音效。|
|A3: 背景音乐轨 (BGM)|承载情绪渲染音乐。|扫描 BGM 的波形峰值（瞬态检测），生成“Beat（节拍点）”。|更换 BGM 风格、开启/关闭“自动音频闪避（Audio Ducking）”。|

            轨道层级
            核心功能
            AI 自动化逻辑
            用户干预维度
            V1: 视觉主轨 (主副分镜)
            承载生成的视频片段/图片垫片。
            系统根据下方的【台词时长】反向计算并锁定视频生成的秒数。
            拖拽调整顺序、替换片段、添加转场效果。
            A1: 台词/人声轨 (Dialogue)
            承载 TTS（文本转语音）生成的角色对白。
            AI 自动区分角色标签（如 @角色A，@角色B），分配不同音色库。
            语速微调、情绪选项（愤怒/悲伤）、手动切片。
            A2: 音效轨 (SFX)
            承载动作音效与环境音。
            提取分镜文本中的动词词向量（如“下雨”、“拔剑”），自动在关键帧插入音效库资产。
            音量大小调节、手动删减误判音效。
            A3: 背景音乐轨 (BGM)
            承载情绪渲染音乐。
            扫描 BGM 的波形峰值（瞬态检测），生成“Beat（节拍点）”。
            更换 BGM 风格、开启/关闭“自动音频闪避（Audio Ducking）”。
---
## 三、 核心难点攻坚：“智能踩点与匹配”的工作流引擎
在 AI 辅助创作中，用户最痛恨的就是“音画不同步”。我们需要设计一套自动化的时序引擎。
## 1. 锚点逻辑（谁决定视频有多长？）
- 传统逻辑：用户设定视频生成 3 秒，然后硬配音，导致要么话没说完，要么画面定格。
- 重构逻辑（推演出的核心结论）：必须以“台词（Dialogue）的时长”作为绝对标尺。
工作流：
1. 第一步（TTS 预演）：用户输入剧本台词 -> AI 生成情感语音 -> 系统读取这段语音的时长为 t=4.2s。
2. 第二步（视频生成倒推）：将 4.2s 参数自动喂给视频生成大模型，生成恰好 4.2s 的画面，或通过**“视频插帧（VFI）”与“循环扩展”**凑齐时长。
## 2. 唇形同步（Lip-sync）的工程化实现（置信度：高）
如果做剧情短剧，口型匹配是刚需，但当前多模态大模型的直接文生口型极不可靠。你需要将视频生成与口型匹配解耦：
1. AI生图/生视频（基础层）：先生成人物面部的视频（保持物理和画风一致）。
2. Wav2Lip/SadTalker 后处理（加工层）：时间轴检测到【视频片段】下方挂载了【该角色的台词音频】，自动在后台触发一次“唇部驱动重绘”，最后渲染出成品片段。这样才能保证动作自然的同时口型对准。
## 3. BGM 智能踩点引擎
- 如何让视频“卡点”？
- 方案设计：用户从素材库选择一首“赛博朋克风/120 BPM”的音乐。平台后台算法（如 Librosa）分析其音频包络线，提取重拍（Kick Drum/Snare）。
- UI 呈现：在时间轴轨道上，BGM 的节拍位置自动生成**“吸附线（Snap Grid）”**。用户拖拽分镜卡片（Storyboard Block）时，片段的首尾会自动吸附到重拍上。
---
## 四、 批判性评估（工程风险预警）
作为架构师，我需要客观提醒，将一个“陈旧的生图平台”升级到拥有“四轨智能非线编时间轴”的平台，工程难度会直线上升。
- 优势：
    - 极强的工业感：这种体验能让用户从“玩票”心态转变为“生产力”心态，他们愿意为成品视频买单，而不仅是为几张有趣的图买单。
- 劣势/风险点：
    - 浏览器性能瓶颈（置信度：高）：如果前端使用传统的 DOM 去渲染这么多轨道、波形图和高频次的视频预览，内存会瞬间爆炸，页面卡死。你需要要求你的前端团队重写底层，使用 Canvas/WebAssembly 或现成的 Web 音视频编辑框架（如 FFmpeg.wasm） 来承载时间轴组件。
    - 状态同步困难（置信度：中）：云端（生成任务排队中）和本地（用户在调整音轨）的状态极难同步。必须设计极其稳健的 WebSocket 长连接方案和防抖动机制。